{{!< main}}

<section class="error">
        <div class="error__content">
            {{> title title='500'}}
            <p>Мы уже фиксим</p>
            {{> link href="chats.hbs" text='Назад к чатам'}}
        </div>
</section>